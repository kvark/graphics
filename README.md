# graphics

A knowledge graph of computer graphics, in which **the edges carry the reasons**.

Most maps of a field give you the nodes: a list of techniques, a reading list, a
citation graph. The nodes are the easy part — GGX, Smith, VNDF are in every
textbook index. What is almost never written down is *why one leads to the next*:

```
GGX  ──"can't hit back-facing normals"──▶  VNDF
```

That edge is the compressed judgment a practitioner accumulates and a citation
database throws away. Collecting those is the entire point of this repository.

## Reading it

The graph itself lives at
**[kvark.github.io/graphics](https://kvark.github.io/graphics/)** — that is the
only rendered view, so there is nothing here to drift out of sync with it. Below
is an index; the nodes themselves are plain YAML, one file per node, in
[`nodes/`](nodes/):

```yaml
title: Height-Correlated Smith
cluster: materials
year: 2014
summary: >
  Accounts for the fact that a microfacet hidden from the light is likely to
  be hidden from the view as well, since both depend on the same height field.
wikipedia: https://en.wikipedia.org/wiki/Specular_highlight
refs:
  - title: Understanding the Masking-Shadowing Function in Microfacet-Based BRDFs
    authors: Heitz
    year: 2014
    url: https://jcgt.org/published/0003/02/03/paper.pdf
edges:
  - rel: corrects
    to: smith-masking
    why: incident and exitant masks are related, not independent
```

There are eight relations and no free-form types: `part-of`, `specializes`,
`approximates`, `corrects`, `extends`, `requires`, `alternative-to`,
`validates`. Every edge except `part-of` must say *why*, and every node must
carry at least one link out — a paper or a Wikipedia article — so a reader can
check a claim instead of trusting the summary. Both rules are enforced by CI.
See [SCHEMA.md](SCHEMA.md).

## Browsing it

Each cluster is one pannable, zoomable diagram; every node gets a page with its
reasoned edges in **both** directions, a view of its neighbourhood, and its
sources. Published automatically from `main`, and served whole as
[`graph.json`](https://kvark.github.io/graphics/graph.json).

```console
$ python3 tools/site.py            # build it locally into site/
$ python3 tools/site.py --vendor   # ...with mermaid bundled, as CI publishes it
```

## Querying it

The question a rendered mind map can never answer is "what connects these two
things?" — so that is the one the tools answer:

```console
$ python3 tools/kg.py path lambert diffuse-layering
Lambertian Diffuse (lambert)
  --specializes-->
      (the simplest case, a constant BRDF)
  Diffuse Reflection (diffuse-reflection)
  --part-of-->
  Reflection (reflection)
  <--part-of--
  Fresnel Equations (fresnel)
  <--requires--
      (each microfacet is a mirror, so needs its reflectance)
  Microfacet Model (microfacet-model)
  ...
```

```console
$ python3 tools/kg.py show vndf       # a node, with incoming and outgoing edges
$ python3 tools/kg.py search fresnel  # substring across ids, titles, summaries
$ python3 tools/kg.py stats           # counts, and anything left unconnected
```

## Contributing

Add one file to `nodes/`. You never have to edit an existing node to attach a
new one, because edges are declared on the node that owns them — so two people
working in the same area do not collide.

Then run `python3 tools/build.py`, which validates the graph and regenerates
everything below this line. CI runs `--check` on every push, and a merge to
`main` republishes the site.

An edge whose `why` could be swapped onto any other pair of nodes is not
carrying its weight. "improves on it" is not a reason; "photon count is capped
by memory, so bias never vanishes" is.

<!-- BEGIN GENERATED -->
<!-- Generated by tools/build.py from nodes/*.yaml. Do not edit by hand. -->

**161 nodes · 182 typed edges · 161 anchored to a source or article · 12 clusters.**

| Cluster | Nodes | |
| --- | --: | --- |
| [Foundations](https://kvark.github.io/graphics/c/foundations.html) | 11 | The quantities everything else is defined in terms of. |
| [Light Transport](https://kvark.github.io/graphics/c/light-transport.html) | 20 | Algorithms that solve the rendering equation, and the estimators they are built from. |
| [Materials and BSDFs](https://kvark.github.io/graphics/c/materials.html) | 29 | Models for what a surface does to light that hits it. |
| [Participating Media](https://kvark.github.io/graphics/c/volumetrics.html) | 9 | Transport through a volume rather than across a surface. |
| [Sampling](https://kvark.github.io/graphics/c/sampling.html) | 9 | Where to put the samples, and how the error is distributed when you cannot afford enough of them. |
| [Real-Time Shading](https://kvark.github.io/graphics/c/real-time-shading.html) | 18 | Getting a usable approximation of the above inside a frame budget. |
| [Shadows](https://kvark.github.io/graphics/c/shadows.html) | 8 | Visibility from the light, and the artifacts of every way of caching it. |
| [Anti-Aliasing and Filtering](https://kvark.github.io/graphics/c/antialiasing.html) | 12 | Reconstruction, in space and over time. |
| [Denoising](https://kvark.github.io/graphics/c/denoising.html) | 5 | Trading variance for bias so that low sample counts are shippable. |
| [Geometry and Intersection](https://kvark.github.io/graphics/c/geometry.html) | 25 | Representing surfaces and finding where a ray meets them. |
| [Color and Tone](https://kvark.github.io/graphics/c/color.html) | 8 | Turning radiance into pixels a display can show. |
| [Neural Representations](https://kvark.github.io/graphics/c/neural.html) | 7 | Learned scene representations, and their reconstruction lineage. |

<!-- END GENERATED -->
